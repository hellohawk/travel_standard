
<% uploads.each do |upload| %>

  <div class="2u">
    <span class="image fit">
      <a href="#"><%= image_tag upload.image.thumb.to_s %></a>
      <%= render partial: "like", locals: { upload: upload } %>
    </span>
  </div>

<% end %>


<!-- ここは多分消す -->
