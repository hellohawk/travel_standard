<!-- ここに画像のdivを入れる -->
  <div class="more">
    <ul class="more_list">
      <li>
        <%= link_to '詳細', "/uploads/#{upload.id}", method: :get %>
      </li>
      <% if user_signed_in? && current_user.id == upload.user_id %>
      <li>
        <%= link_to '編集', "/uploads/#{upload.id}/edit", method: :get %>
      </li>
      <li>
        <%= link_to '削除', "/uploads/#{upload.id}", method: :delete %>
      </li>
    </ul>
  </div>
<% end %>
  <%= simple_format(upload.continents) %>
<span class="name">
  <a href="/users/<%= upload.user_id %>">
    <span>投稿者</span><%= upload.user.nickname %>
  </a>
</span>