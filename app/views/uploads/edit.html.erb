  <div class="contents row">
    <%= form_tag("/uploads/#{@upload.id}", method: :patch ) do %>
      <h3>
        編集する
      </h3>
<!--       <input placeholder="Image Url" type="text" name="image" value="<%= @upload.image %>" autofocus="true">
 -->
 <%= form_for @upload,:url => {:action => 'update'}, :html => {:multipart => true} do |f| %>

  <%= f.file_field :image %><br>
  <%= f.submit %>

<% end %>
       <textarea cols="30" name="comment" placeholder="comment" rows="10"><%= @upload.comment %></textarea>
      <input type="submit" value="SENT">
    <% end %>
  </div>